
{{>header}}





<!-- Page Content -->

  {{!-- <div class="container-fluid banner gallery">
  <div class="row">
    <div class="col-md-12">
      <nav class="navbar navbar-md">
        <div class="navbar-brand">INSTARTIST</div>
        <ul class=" nav">
          <li class="nav-item active">
            <a class="nav-link" href="/">Gallery <span class="sr-only">(current)</span></a>
          </li>
          {{#if loggedIn}}
          <li class="nav-item">
            <a class="nav-link" href="/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a id="logout" class="nav-link" href="#">Logout</a>
          </li>
          {{else}}
          <li class="nav-item">
            <a class="nav-link" href="/signup">Signup</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li>
          {{/if}}
        </ul>
      </nav>
    </header>

  <h1 class="font-weight-light text-center text-lg-left mt-4 mb-0">Our Work</h1>
    </div>
  <hr class="mt-2 mb-5">

  <div class="row text-center text-lg-left">

    {{#each posts}}
      <div class="col-lg-3 col-md-4 col-6">
        <a href="/post/{{id}}" class="d-block mb-4 h-100">
          <img class="img-fluid img-thumbnail" src="{{img_url}}" alt="">
        </a>
      </div>
    {{/each}} --}}

  